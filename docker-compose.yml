version: '3.8'

services:
  kiro-rs:
    build: .
    ports:
      - "8990:8990"
    volumes:
      - ./config.json:/app/config.json:ro
      - ./credentials:/app/credentials
    restart: unless-stopped
    environment:
      - RUST_LOG=info
